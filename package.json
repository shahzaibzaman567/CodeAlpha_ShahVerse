{
  "name": "shahverse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
